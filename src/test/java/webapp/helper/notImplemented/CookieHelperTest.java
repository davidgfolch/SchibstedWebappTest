package webapp.helper.notImplemented;

import static org.junit.Assert.*;

import org.junit.Test;

import com.schibsted.webapp.server.helper.CookieHelper;
import com.schibsted.webapp.server.helper.EncryptHelper;


public class CookieHelperTest {
	
//	private HttpExchangeMock ex=new HttpExchangeMock();
	
	@Test
	public void test() {
//		CookieHelper.getCookie(ex, cookieName);
//		CookieHelper.getCookie(cookieHeaders, cookieName)
//		CookieHelper.setCookie(ex, cookie, value);
//		CookieHelper.setCookie(ex, cookie, value, httpOnly, expires);
//		assertTrue("Encription don't work",cond);
	}

}
