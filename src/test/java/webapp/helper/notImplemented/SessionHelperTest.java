package webapp.helper.notImplemented;

import static org.junit.Assert.assertTrue;

import org.junit.Test;

import com.schibsted.webapp.controller.LoginController;
import com.schibsted.webapp.server.helper.ReflectionHelper;
import com.schibsted.webapp.server.helper.SessionHelper;

public class SessionHelperTest {


//	@Test
//	public void hasDefaultConstructor() {
//		assertTrue(SessionHelper.getSession(ex));
//		assertTrue(SessionHelper.invalidateSession(session));
//		assertTrue(SessionHelper.isAuthenticated(ex));
//		assertTrue(SessionHelper.isSessionTimedOut(session));
//	}

}
